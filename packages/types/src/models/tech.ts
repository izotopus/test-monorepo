/* export interface GlossaryItem {
  term: string;
  definition: string;
}

export interface TechItem {
  name: string;
  slug: string;
  desc: string;
  content: string;
  icon: string;
  docsUrl?: string;
  glossary?: GlossaryItem[];
  coverImage?: string;
} */